export function EntradaValor () {

    function input ()
    {
            <input type="number" placeholder="Digite o valor da compra" id="input" />

            const input = document.getElementById('input');
            const valor = input.value;

            return ( valor)
    }
    
    return (valor)
}